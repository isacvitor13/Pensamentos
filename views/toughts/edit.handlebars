<div class="thoughts-form-control">
    <a href="/toughts/dashboard">Voltar</a>
    <h1>Editando um pensamento</h1>
    <form action="/toughts/edit" method="post">
    <input type="hidden" name="id" id="{{tought.id}}" value="{{tought.id}}">
        <div class="form-control">
            <label for="title">Pensamentos</label>
            <input type="text" name="title" placeholder="Digite seu pensamento" value="{{tought.title}}"></div>
    <input type="submit" value="editar pensamento">
    </form>
</div>